import { ShellIcon } from 'lucide-react'

export const Logo = () => {
  return (
    <span className="w-fit flex items-center gap-0.5 font-bold text-xl text-foreground tracking-tighter">
      <ShellIcon className="text-foreground size-5" />
      anota.aí
    </span>
  )
}
